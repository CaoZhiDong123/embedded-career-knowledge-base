# 学习优先级

## P0：近期最高优先级

### ⭐ 嵌入式实时软件结构

- ISR 保持短小、执行时长确定；耗时工作下放主循环或 Task。
- DMA 与中断协作：`DRDY -> SPI DMA -> DMA 完成 -> Buffer/Queue -> Task/Main 解算`。
- `volatile`、原子性、临界区与整帧数据一致性。
- 双缓冲、环形缓冲与 Queue 的适用场景。
- UART ORE、丢字节、阻塞 `printf`、CPU 负载与实时性。

证据：2026-09-07 维特智能真实技术笔试的 A6、D3、D4 连续考查该链路；A6 有正确意识，但 D3 分析不完整、D4 基本未答。该主题同时与 STM32/FreeRTOS 项目高度相关。

## P1：短期重点

### ⭐ 通信协议工程细节

- 大小端与多字节拼接。
- I2C 7 位地址左移及 R/W 位：`0x68 -> 0xD0/0xD1`。
- SPI 的 CPOL/CPHA、CS、`WHO_AM_I` 与示波器/逻辑分析仪排障。
- UART 帧头/长度/数据/校验状态机，以及 8N1 吞吐与波特率。

### ⭐ IMU / 姿态基础

学习路径：

`Accel/Gyro/Mag 分别测什么`
`-> 陀螺零偏/噪声/温漂`
`-> 积分漂移`
`-> Roll/Pitch/Yaw`
`-> accel 为何能约束 Roll/Pitch 但不能单独约束 Yaw`
`-> 磁力计航向参考`
`-> ZYX 欧拉角`
`-> Pitch ±90° 万向节死锁`
`-> 单位四元数`
`-> 四元数积分与归一化`
`-> 互补滤波`
`-> Mahony/Madgwick`
`-> 有实际需求后再学习 EKF`

## P2：长期持续巩固

- C 语言：指针的地址/值/解引用，位运算，`sizeof/strlen`，整数范围与补码，`static/const/volatile/extern`。
- 数据布局：结构体 padding、对齐与序列化。
- 定时器：`T = (PSC+1)(ARR+1)/f_TIM`。
- STM32 / MCU：输入捕获、ADC、看门狗。
- PID、RTK/GNSS/组合导航、Linux、调试与硬件排障。

## 当前低优先级

- ROS2 高级内容
- EKF 复杂数学推导
- 高级 Linux 内核
- 与近期技术考核无关的扩展内容

## 调整规则

优先级随新 JD、HR/技术反馈、真实笔试/面试问题和训练暴露的差距动态升降级；变化必须写明证据来源与日期。一次答对只构成单次证据，不直接判定完全掌握。
